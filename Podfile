# Uncomment the next line to define a global platform for your project
# platform :ios, '10.0'

target 'Dividend' do
    pod 'Kanna', '~> 4.0.0'

   target 'DividendTests' do
        inherit! :search_paths
    end
end
